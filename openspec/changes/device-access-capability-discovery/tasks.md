## 1. 数据模型与迁移

- [x] 1.1 定义设备状态、任务状态、任务类型和标准错误码常量，覆盖连接测试与能力发现闭环。
- [x] 1.2 设计凭据服务边界，支持保存认证材料并返回 `credential_ref`，业务层只能通过引用解析运行时凭据。
- [x] 1.3 调整 SQLAlchemy 模型，连接配置保存认证信息引用，任务状态保存关联设备、结果摘要、错误码、错误消息和完成时间。
- [x] 1.4 新增最后发现结果模型，保存设备、来源任务、capabilities、基础系统信息、发现时间和摘要。
- [x] 1.5 编写 Alembic 迁移，创建或调整连接配置、任务状态和发现结果相关表结构。
- [x] 1.6 补充 Repository 方法，支持按设备读取连接配置、写入发现结果、更新设备状态和更新任务状态。

## 2. API 与 Schema

- [x] 2.1 更新设备创建和读取 schema，允许提交认证信息但读取响应不得返回密码或密钥明文。
- [x] 2.2 更新设备创建服务，保存认证材料到凭据服务边界，并在连接配置中保存认证信息引用。
- [x] 2.3 新增 `POST /api/v1/devices/{device_id}/connection-test`，创建连接测试任务并返回任务标识。
- [x] 2.4 新增 `POST /api/v1/devices/{device_id}/capability-discovery`，创建能力发现任务并返回任务标识。
- [x] 2.5 增强任务查询 schema 和接口，返回任务状态、关联设备、结果摘要、错误码、错误消息和脱敏上下文。
- [x] 2.6 处理设备不存在、连接配置缺失和凭据不可用的 API 错误响应，确保不会投递无效任务。

## 3. NETCONF 服务与错误模型

- [x] 3.1 扩展 NETCONF 错误类型，映射设备不可达、连接超时、认证失败、NETCONF 协议错误、凭据不可用和内部错误。
- [x] 3.2 调整 ncclient 适配层，统一处理超时、认证失败、SSH 连接异常和 NETCONF RPC 或 hello 协议异常。
- [x] 3.3 在 NETCONF 服务层实现连接测试方法，输入为设备连接参数和运行时凭据，输出标准成功或错误结果。
- [x] 3.4 在 NETCONF 服务层实现能力发现方法，读取 server capabilities，并在设备支持时读取基础系统信息。
- [x] 3.5 确保 NETCONF 服务和适配层不会在异常消息、返回值或日志中暴露认证明文。

## 4. Celery 任务闭环

- [x] 4.1 扩展任务服务，支持提交设备连接测试和能力发现任务，写入 `queued` 状态并投递 Celery。
- [x] 4.2 新增 Celery task 执行连接测试，启动时置为 `running`，成功置为 `succeeded`，失败置为 `failed` 并记录标准错误。
- [x] 4.3 新增 Celery task 执行能力发现，成功后写入最后发现结果并更新设备状态。
- [x] 4.4 统一任务状态更新事务，确保任务结果、设备状态和发现结果写入失败时可回滚或记录失败。
- [x] 4.5 为 API 投递、Worker 开始、成功和失败路径输出结构化日志字段。

## 5. 测试

- [x] 5.1 增加模型和 Repository 测试，验证设备、连接配置、认证信息引用、任务状态和发现结果可持久化。
- [x] 5.2 增加 API 测试，验证连接测试和能力发现接口会投递任务，并且响应不泄漏密码或密钥。
- [x] 5.3 增加 NETCONF 服务单元测试，使用 fake client 覆盖连接成功、能力发现成功和标准错误映射。
- [x] 5.4 增加 Celery task 直接执行测试，验证 `running`、`succeeded`、`failed` 状态和错误字段回写。
- [x] 5.5 增加日志脱敏测试，验证任务 metadata 和日志上下文不包含认证明文。
- [x] 5.6 更新远端 NETCONF mock Server 集成测试，默认使用 `172.16.5.38:830`、用户名 `netconf`、密码 `netconf`，并支持环境变量覆盖或跳过。

## 6. 验证与文档

- [x] 6.1 更新后端 README 或开发说明，记录设备接入、连接测试、能力发现和任务查询的本地调用方式。
- [x] 6.2 记录 NETCONF mock Server 集成测试的环境变量、默认参数和跳过策略。
- [x] 6.3 执行后端测试套件，确认单元测试、API 测试和可配置集成测试通过。
- [x] 6.4 执行 OpenSpec 校验，确认 proposal、design、spec 和 tasks 均满足 schema 要求。
